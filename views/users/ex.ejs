<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Document</title>
  </head>
  <body>
    <form
      action="/users/create_image"
      method="post"
      enctype="multipart/form-data"
    >
      <input type="file" name="imgFile" />
      <input type="submit" value="파일 업로드하기" />
    </form>

    <% if(locals.url) {%>
    <img src="/images/<%= url %>" alt="image" />
    <% } %>
  </body>
</html>
